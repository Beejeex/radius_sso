---
name: tdd-vertical-slices
description: Plan and implement one independently testable vertical slice at a time.
---

# TDD Vertical Slices

Define one user or system outcome, write the smallest failing or expected test set, implement only what it requires, and validate the slice across the boundaries it actually uses. Keep later slices blocked until the current slice is green unless the user accepts the batching risk and follow-up.
